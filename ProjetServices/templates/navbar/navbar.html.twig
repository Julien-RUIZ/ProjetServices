
<nav class="navbar navbar-expand-lg bg-body-tertiary border-bottom border-body">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{path('app_home') }}">Projet-services</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        {% if is_granted("ROLE_USER") %}
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link {% if app.request.pathInfo == '/'  %} active {% endif %}" aria-current="page" href="{{path('app_home') }}">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if app.request.pathInfo == '/services'  %} active {% endif %}" href="{{ path('app_service_management') }}">Services</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if app.request.pathInfo == '/note'  %} active {% endif %}" href="{{ path('app_note') }}">Notes</a>
                </li>
            </ul>
        </div>
        {% else %}
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link {% if app.request.pathInfo == '/'  %} active {% endif %}" aria-current="page" href="{{path('app_home') }}">Home</a>
                </li>
            </ul>
        </div>
        {% endif %}
        <div class="loginlogout">
            <div class="collapse navbar-collapse" id="navbarNav" style="justify-content: end;">
                {% if is_granted("ROLE_ADMIN") %}
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('app_admin') }}">Admin</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('app_profile') }}">Profil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('app_logout') }}">Logout</a>
                    </li>
                </ul>
                {% elseif is_granted("ROLE_USER") %}
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link active" href="{{ path('app_profile') }}">Profil</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link active" href="{{ path('app_logout') }}">Logout</a>
                        </li>
                    </ul>
                {% else %}
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link active" href="{{ path('app_login') }}">Login</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link active" href="{{ path('app_register') }}">Register</a>
                        </li>
                    </ul>
                {% endif %}
            </div>
        </div>
    </div>
</nav>


